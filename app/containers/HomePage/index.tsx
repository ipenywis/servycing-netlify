import React from "react";
import { hot } from "react-hot-loader/root";

interface IHomePageProps {}

function HomePage(props: IHomePageProps) {
  return <div>Welcome to servycing...</div>;
}

export default hot(HomePage);
